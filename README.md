# VICTMATE

Distraction-free workspace with a Pomodoro timer, YouTube/local study media, PDF reader, ambient soundscape controls, and persistent notes/tasks.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
